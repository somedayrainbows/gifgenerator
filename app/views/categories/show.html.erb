<h1 align="center" >Gifs in the <%= @category.name %> category</h1>

<div align="center" class="gifs">
  <% @category.gifs.each do |gif| %>
    <iframe src="<%= gif.image_path %>" width="480" height="270" frameBorder="0" class="giphy-embed" allowFullScreen></iframe><br>
  <% end %>
</div>

<p><%= link_to "<< back to all categories", categories_path %></p>
<br><br>
